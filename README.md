# android_device_motorola_scout-kernel

Prebuilt GKI 6.1 kernel, modules, and UAPI headers for Motorola Edge 60 Fusion (scout / MT6878).

Reconstructed from stock firmware. Used as `TARGET_PREBUILT_KERNEL` + `TARGET_PREBUILT_KERNEL_HEADERS` by `device/motorola/scout`.
